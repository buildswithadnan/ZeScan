<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 
        PERMISSIONS REQUIRED BY ZESCAN:
        
        1. CAMERA
           - Purpose: To scan documents using the device camera
           - When used: When user taps "Start Scanning" or uses ML Kit scanner
           - Required: Yes (core feature)
        
        2. READ_EXTERNAL_STORAGE (Android 12 and below)
           - Purpose: To access and import images from gallery
           - When used: When user taps "Import from Gallery"
           - Required: Optional (only for gallery import feature)
           - Note: Only used on Android 12 (API 32) and below
        
        3. READ_MEDIA_IMAGES (Android 13+)
           - Purpose: To access and import images from photo library
           - When used: When user taps "Import from Gallery"
           - Required: Optional (only for gallery import feature)
           - Note: Replaces READ_EXTERNAL_STORAGE on Android 13+ (API 33+)
        
        INTERNET (automatically granted):
           - Purpose: For feedback/bug report submission
           - When used: When user submits feedback from settings
           - Required: Optional (only for feedback feature)
    -->
    
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    
    <application
        android:label="ZeScan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
